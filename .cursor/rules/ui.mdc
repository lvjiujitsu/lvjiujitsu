---
description: Templates, CSS, JavaScript e fluxos visuais do LV
globs: "{templates/**/*.html,static/system/**/*.css,static/system/**/*.js}"
alwaysApply: false
---

Use `/lv-ui-delivery`.

Leia `docs/UI-SCREEN-CONTRACT.md` e o PRD do fluxo.

Apresente a proposta visual e aguarde aprovação antes de implementar. Depois, valide no browser interno disponível em desktop e mobile e registre evidência.
