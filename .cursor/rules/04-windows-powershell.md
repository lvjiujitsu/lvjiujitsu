# Windows PowerShell Rule

- Preferir `\.venv\Scripts\python.exe`.
- Não usar `source`.
- Não depender de `&&` como separador padrão em PowerShell.
- Garantir UTF-8 e ExecutionPolicy antes de operações relevantes.
