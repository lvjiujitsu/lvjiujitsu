---
description: Arquitetura, persistência e testes Django do LV JIU JITSU
globs: "**/*.py"
alwaysApply: false
---

Use `/lv-django-delivery`.

Respeite Django MVT com `services/` e `selectors/`, validação server-side,
permissões no backend e a política operacional de `AGENTS.md`.

Escreva o teste antes do código quando houver comportamento testável e
execute-o quando proporcional ao escopo local.
